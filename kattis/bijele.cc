#include <iostream>

using namespace std;

int main (void) {
    int n;
    cin >> n;
    cout << (1 - n);
    cin >> n;
    cout << " " << (1 - n);
    cin >> n;
    cout << " " << (2 - n);
    cin >> n;
    cout << " " << (2 - n);
    cin >> n;
    cout << " " << (2 - n);
    cin >> n;
    cout << " " << (8 - n) << endl;
}
